{
  "buildCommand": "cd aura-studio && npm install && npm run build",
  "outputDirectory": "aura-studio/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}

